# Ubuntu Installation

If you're on Ubuntu, you can run `./distro_scripts/ubuntu/ubuntu_apt_helper.sh` to install all the required packages (this requires `sudo`), then run `./distro_scripts/ubuntu/ubuntu_setup.sh` to install Rottnest itself into a virtual environment in the current directory.
